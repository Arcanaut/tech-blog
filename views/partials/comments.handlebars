<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{User.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
    <div class="createdBy">

    </div>
  </section>
  {{/each}}
</div>